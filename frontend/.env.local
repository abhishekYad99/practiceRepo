# Local-only (gitignored). Points the frontend at the backend running on the
# host machine's LAN IP so teammates on the same network can reach it.
# NEXT_PUBLIC_API_BASE_URL=http://192.168.0.131:4000/api

NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api
